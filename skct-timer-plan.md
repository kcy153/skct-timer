# SKCT 인지역량 연습 타이머 – 안드로이드 앱 계획서

> 이 문서는 Claude Code가 읽고 앱을 만들기 위한 계획서입니다.
> 아래 **범위 밖** 항목은 사용자가 명시적으로 뺀 기능이니 임의로 추가하지 마세요.
> 문서에 없는 결정이 필요하면 임의로 정하지 말고 사용자에게 먼저 물어보세요.

---

## 1. 목적

SKCT 인지역량 시험(5개 영역 × 15분, 영역 사이 1분 휴식)을 실제와 같은 시간 흐름으로 연습하기 위한 타이머 앱.
개인 연습용이며 스토어에 등록하지 않고, APK를 QR로 받아 설치한다.

## 2. 시험 구성 (확인된 사실)

| 항목 | 값 |
|---|---|
| 영역 수 | 5개 |
| 영역별 시간 | 15분 (20문항) |
| 영역 사이 휴식 | 1분 (예시문항 화면이 나오는 구간, 사용자가 직접 확인) |
| 총 시간 (기본값) | 15분 × 5 + 1분 × 4 = **79분** |
| 영역 순서 (가정) | 언어이해 → 자료해석 → 창의수리 → 언어추리 → 수열추리 |

- 영역 순서는 나무위키 나열 기준이며 실제 출제 순서는 확인되지 않았다. **코드 한 곳(상수 목록)에서만 정의**해서 나중에 바꾸기 쉽게 한다.
- 영역 시간(15분)과 영역 이름은 설정 화면에 노출하지 않고 상수로 둔다.

## 3. 범위

### 만들 것
- 시험 흐름 자동 진행(카운트다운 → 영역 5개 + 사이 휴식 → 종료)
- 알림음/진동 (4절 규칙)
- 일시정지 / 재개
- 설정 2가지: **휴식 시간 조절**, **소리 / 진동 선택**
- 시험 중 화면 항상 켜짐, 백그라운드에서도 정확한 타이밍

### 범위 밖 (하지 말 것)
- 문항 페이스 가이드 (지금쯤 몇 번 등)
- 결과 화면, 영역별 사용 시간 요약, 기록 저장
- 영역 건너뛰기 / 일찍 끝내기 (실제 시험이 불가능하고 시간이 부족해서 필요 없음)
- 첫 영역 전 휴식 포함 옵션
- 영역 시간·문항 수·영역 순서를 바꾸는 설정 UI
- 계정, 서버, 광고, 분석, 네트워크 통신 일체

## 4. 알림 규칙 (핵심 요구사항)

**경계(boundary)** 는 다음 시점을 말한다.

1. 시험 시작 (카운트다운이 끝나고 첫 영역이 시작되는 순간)
2. 영역 종료 = 휴식 시작
3. 휴식 종료 = 다음 영역 시작
4. 시험 종료 (5번째 영역 종료)

### 4-1. 경계 알림
모든 경계 B에 대해 다음을 울린다.

| 시점 | 소리 | 진동 |
|---|---|---|
| B − 3초 | 예비음(낮은음) | 짧게 |
| B − 2초 | 예비음 | 짧게 |
| B − 1초 | 예비음 | 짧게 |
| B (정각) | **경계음(예비음보다 높은음)** | 길게 |

- 시험 시작 경계는 시작 버튼을 누른 뒤 3초 카운트다운과 겹친다. 화면에 3, 2, 1이 보이며 예비음이 울리고, 0에서 경계음이 울리면서 첫 영역이 시작된다. (이 3초 카운트다운은 "시험 시작 3,2,1초 알림"을 구현하기 위해 추가한 것이다.)
- 휴식 시간이 0초로 설정되면 영역 종료와 다음 영역 시작이 같은 경계이므로 **경계 알림을 한 번만** 울린다(중복 금지).

### 4-2. 시험 중 알림
- 각 **영역 종료 3분 전**(영역 시작 후 12분 지점)에 알림음 **1번**. 예비음 없음.
- 휴식 구간에서는 울리지 않는다.
- 진동을 켠 경우 중간 길이로 1번.

### 4-3. 음 설계 (상수로 분리, 나중에 조정 가능하게)
소리는 오디오 파일 없이 사인파를 코드로 생성한다.

| 종류 | 주파수(초안) | 길이(초안) |
|---|---|---|
| 예비음 | 660 Hz | 120 ms |
| 3분 전 알림음 | 880 Hz | 400 ms |
| 경계음 | 1320 Hz | 600 ms |

- 경계음은 반드시 예비음보다 높아야 한다. 나머지 값은 사용자가 실제로 들어보고 바꿀 수 있다.
- 시작/끝에 클릭 노이즈가 나지 않도록 짧은 페이드인/아웃을 준다.
- 진동 패턴도 상수로 분리한다: 예비(짧게), 3분 전(중간), 경계(길게).

## 5. 화면과 상태

### 상태
`Idle → Countdown(3초) → Section(1~5) ⇄ Break → Finished`
`Paused`는 위 어느 실행 상태에서든 들어갈 수 있는 오버레이 상태다.

### 화면
1. **시작 화면**: 전체 일정(영역 5개 + 휴식, 총 시간)을 보여주고 시작 버튼, 설정 진입 버튼.
2. **시험 화면**: 남은 시간 MM:SS (가장 크게), 현재 영역 이름, 진행 표시(영역 5칸 + 휴식 구간), 일시정지/재개 버튼 하나.
3. **휴식 화면**: 남은 휴식 시간과 "다음: ○○" 표시. 색은 시험 화면과 구분되는 차분한 톤.
4. **종료 화면**: "시험 종료" 문구와 처음으로 버튼만. 결과 요약은 넣지 않는다.
5. **설정 화면**: 휴식 시간 조절, 소리 켜기/끄기, 진동 켜기/끄기.

### 화면 동작 규칙
- 남은 시간은 **올림(ceil)** 으로 표시한다. 영역 시작 직후 15:00, 마지막 1초는 0:01이며 경계 정각에 0:00이 된다. 예비음은 표시가 3, 2, 1로 바뀌는 순간에 울린다.
- 영역 종료 3분 전부터 남은 시간 숫자를 앰버 색으로 바꾼다(시각적 경고).
- 시험 중 뒤로가기를 누르면 "시험을 중단할까요?" 확인창을 띄운다. 확인하면 Idle로 돌아간다.
- 시험 중에는 화면이 꺼지지 않게 한다(`FLAG_KEEP_SCREEN_ON`).
- 세로 화면 고정.

### 설정 화면 세부
- **휴식 시간**: 0초 ~ 5분, 10초 단위, 기본 60초. 시험 진행 중에는 변경 불가(시작 전에만).
- **소리 / 진동**: 독립된 두 스위치. 둘 다 켜기, 하나만 켜기, 둘 다 끄기 모두 허용. 저장 필요(SharedPreferences 또는 DataStore).
- 진동을 지원하지 않는 기기에서는 진동 스위치를 비활성화하고 안내 문구를 보여준다.

## 6. 디자인 방향

시험 직전의 집중을 방해하지 않는 차분한 인상이 목표. 한 화면에서 눈에 띄는 것은 **남은 시간 숫자 하나**로 하고 나머지는 조용하게 둔다.

| 이름 | 용도 | 값(초안) |
|---|---|---|
| Navy | 다크 배경 / 라이트 텍스트 | `#0B1626` |
| Ice | 다크 텍스트 / 라이트 배경 | `#E6EDF5` |
| Paper | 라이트 배경 | `#F5F7FA` |
| Teal | 진행 표시, 주요 버튼 | `#2BB6A3` |
| Amber | 마지막 3분 경고 | `#F2A93B` |

- 라이트/다크 모두 지원(시스템 설정 따름). 배경, 텍스트, 강조색은 테마 토큰으로 정의한다.
- 타이포는 산세리프 한 종류. 남은 시간은 **고정폭 숫자(tabular numerals)** 로 크게 표시해서 자릿수 변화에 숫자가 흔들리지 않게 한다. (Compose: `fontFeatureSettings = "tnum"`)
- 영역 이름은 한글이므로 한글이 깔끔한 폰트를 쓴다. 기본 시스템 폰트로 충분하다. 원하면 Pretendard(OFL)를 번들해도 된다.
- 장식용 그라디언트, 카드 남발, 불필요한 애니메이션은 넣지 않는다. 상태 전환(영역 → 휴식)에서만 색이 바뀌는 정도로 절제한다.
- 색만으로 상태를 구분하지 않는다. 휴식 화면에는 "휴식"이라는 텍스트가 반드시 있어야 하고, 텍스트 대비는 WCAG AA 이상을 지킨다.

## 7. 기술 스택 (결정)

**Kotlin + Jetpack Compose 네이티브 앱**

선택 이유: 이 앱의 핵심은 "화면이 꺼져도 정확한 시각에 소리와 진동이 나는 것"이라, 포그라운드 서비스, 웨이크락, 오디오, 진동을 안정적으로 다룰 수 있는 네이티브가 가장 확실하다.

| 항목 | 값 |
|---|---|
| 언어 / UI | Kotlin, Jetpack Compose, Material 3 |
| minSdk | 26 (Android 8.0) |
| targetSdk | 빌드 시점의 최신 안정 버전 |
| 빌드 | Gradle (Kotlin DSL) |
| 패키지명(초안) | `com.example.skcttimer` (사용자가 바꾸면 따른다) |
| 앱 이름 | SKCT 타이머 |
| 저장소 | SharedPreferences 또는 DataStore (설정 3개만) |
| 외부 라이브러리 | 최소화. 네트워크 라이브러리 불필요 |

## 8. 구현 설계

### 8-1. 타임라인을 순수 함수로 만든다 (가장 중요)
UI, 서비스, 안드로이드 API와 분리된 순수 Kotlin 모듈로 다음을 만든다.

```
buildTimeline(sectionCount, sectionSec, breakSec) -> List<Segment>
// Segment(type: Countdown|Section|Break, index, startSec, endSec)

buildAlerts(timeline) -> List<Alert>   // 시각(초) 오름차순
// Alert(timeSec, kind: PRE | BOUNDARY | MID)
```

- Countdown 3초, Section 900초, Break 설정값(0이면 Break 세그먼트를 만들지 않음)을 순서대로 배치한다.
- 경계 시각 집합을 구해서 중복을 제거한 뒤 각 경계에 PRE(B−3, B−2, B−1)와 BOUNDARY(B)를 만든다.
- 각 Section 종료 180초 전에 MID를 만든다.
- 이 로직은 **단위 테스트로 검증**한다(10절).

### 8-2. 시간 측정
- 기준 시계는 `SystemClock.elapsedRealtime()` (벽시계 변경, 절전에 영향받지 않음).
- `활성 경과 = now − 시작시각 − 누적 일시정지 시간` (일시정지 중에는 정지 시점 값으로 고정).
- 알림은 "다음 알림 시각까지 남은 시간만큼 `delay`" 후 발사하는 방식으로 처리한다. 고정 주기 폴링만 쓰지 않는다(정확도를 위해). 허용 오차는 ±50ms 이내.
- 화면 표시는 별도 UI 틱(약 100ms)으로 활성 경과에서 계산한다. 서비스와 UI가 각자 시간을 세지 않고 **같은 시작시각/일시정지 상태를 공유**한다.

### 8-3. 일시정지 / 재개
- 일시정지: 정지 시점을 기록하고 예약된 알림을 취소한다.
- 재개: 정지되었던 시간을 누적 일시정지 시간에 더하고 남은 알림을 다시 예약한다.
- 일시정지 중에도 화면은 켜져 있고 서비스는 유지된다.

### 8-4. 백그라운드 동작
- 시험 시작 시 **포그라운드 서비스**를 시작한다. 알림 채널을 만들고 상시 알림에 현재 영역과 남은 시간을 표시한다. 알림을 누르면 앱으로 돌아온다. (남은 시간은 알림 크로노미터 카운트다운을 쓰고, 세그먼트가 바뀔 때 알림 내용만 갱신하는 방식을 권장)
- 서비스에서 `PARTIAL_WAKE_LOCK`을 잡고 시험이 끝나거나 중단될 때 반드시 해제한다.
- Android 14 이상은 포그라운드 서비스 타입 선언이 필수다. `mediaPlayback` 또는 `specialUse` 중 최신 문서를 확인해서 적합한 것을 쓴다.
- Android 13 이상은 알림 권한(`POST_NOTIFICATIONS`)을 첫 실행 때 요청한다. 거부해도 앱은 동작해야 하고, 포그라운드 서비스 알림이 안 보일 수 있다는 안내를 한 번 보여준다.
- 앱이 화면에 있는 동안은 `FLAG_KEEP_SCREEN_ON`으로 화면을 유지한다.
- 사용자에게 배터리 최적화 제외를 권하는 안내 문구를 시작 화면에 작게 넣는다(강제 아님).

### 8-5. 소리와 진동
- 소리는 앱 시작 시(또는 설정 반영 시) 사인파 PCM 버퍼를 미리 만들어 두고 `AudioTrack`(static 모드)으로 재생한다. 재생 지연을 줄이기 위해 알림 직전에 만들지 않는다.
- 오디오 속성은 `USAGE_MEDIA`를 기본으로 한다. 알람 볼륨을 쓰는 방식은 채택하지 않는다.
- 진동은 `VibrationEffect.createWaveform` 등으로 패턴을 만들고, 기기가 진동을 지원하지 않으면 무시한다.
- 소리/진동 스위치가 꺼져 있으면 해당 출력만 건너뛴다. 타이머 진행에는 영향이 없어야 한다.

### 8-6. 구조(제안)

```
app/src/main/java/.../
  domain/        Timeline.kt, Alerts.kt, ExamConfig.kt   ← 순수 Kotlin, 테스트 대상
  service/       TimerService.kt, TimerEngine.kt          ← 포그라운드 서비스 + 엔진
  audio/         ToneGenerator.kt, Haptics.kt
  data/          SettingsRepository.kt
  ui/            theme/, StartScreen, ExamScreen, BreakScreen, FinishedScreen, SettingsScreen
  MainActivity.kt
```

## 9. 빌드와 QR 설치

목표: APK 다운로드 URL을 QR로 만들어서 폰 카메라로 찍으면 APK를 받고 설치하는 흐름.

### 9-1. 서명
- 개인 배포용 **릴리스 keystore를 한 번만 생성**하고 계속 같은 것을 쓴다. 키가 바뀌면 기존 앱 위에 업데이트 설치가 안 된다.
- keystore와 비밀번호는 저장소에 **절대 커밋하지 않는다**(`.gitignore`). 사용자에게 백업 위치를 안내한다.
- 서명 설정은 `local.properties` 또는 환경변수에서 읽는다.

### 9-2. APK 만들기
- `./gradlew assembleRelease`로 서명된 릴리스 APK를 만든다. (로컬에 Android SDK가 없으면 사용자에게 알리고 GitHub Actions 빌드로 대체한다.)
- 파일명 예: `skct-timer-v1.0.0.apk`

### 9-3. 호스팅 (기본안)
**GitHub Releases에 APK를 올리고 그 직접 다운로드 URL을 QR로 만든다.**
- 저장소가 비공개이면 로그인 없이 받을 수 없으므로, 공개 저장소로 두거나 다른 호스팅을 쓴다. 어떻게 할지는 사용자에게 물어본다.
- 대안: 개인 클라우드나 정적 호스팅 링크. 직접 다운로드가 되는 URL이어야 한다.

### 9-4. QR 생성
- URL을 QR PNG로 만드는 스크립트를 함께 제공한다(Python `qrcode` 라이브러리 등). 산출물은 `dist/install-qr.png`.
- 버전을 올려서 릴리스할 때 URL과 QR을 다시 만들 수 있게 한 줄 명령으로 정리한다.

### 9-5. 폰 설치 절차 (README에 그대로 적는다)
1. 폰 카메라로 QR을 스캔하고 링크를 연다.
2. APK를 다운로드한다. (브라우저의 "이런 파일은 기기에 해를 끼칠 수 있음" 경고가 나오면 "그래도 다운로드"를 선택)
3. 다운로드한 APK를 열고, "출처를 알 수 없는 앱 설치" 허용을 묻는 창이 뜨면 해당 브라우저에 대해 허용한다.
4. 설치 후 첫 실행에서 알림 권한을 허용한다.
5. 업데이트할 때는 같은 방식으로 새 APK를 설치하면 덮어써진다.

## 10. 테스트와 완료 기준

Claude Code는 소리를 직접 들을 수 없으므로, **타임라인/알림 스케줄 로직을 단위 테스트로 검증**하고 나머지는 사용자용 수동 테스트 목록으로 넘긴다.

### 10-1. 단위 테스트 (필수, 자동)
기본값(15분, 휴식 60초)에서 확인할 것:
- 총 길이가 3(카운트다운) + 4,500(영역) + 240(휴식) 초인지.
- 경계 개수가 정확한지(시작 1 + 영역 종료/휴식 종료 8 + 종료 1 = 10개).
- 모든 경계마다 B−3, B−2, B−1에 PRE, B에 BOUNDARY가 있는지.
- MID가 각 영역 종료 180초 전에 5번만 있고 휴식 구간에는 없는지.
- 휴식 0초일 때 경계가 중복되지 않고 Break 세그먼트가 없는지.
- 휴식 10초, 5분에서도 알림 시각이 겹치거나 뒤바뀌지 않는지.
- 알림 목록이 시각 오름차순인지.

### 10-2. 개발용 빠른 실행 (디버그 빌드 전용)
- 디버그 빌드에서만 모든 시간을 1/60로 줄이는 배율 옵션을 둔다(15분 → 15초). 릴리스 빌드와 설정 화면에는 노출하지 않는다.
- 이 옵션으로 전체 흐름(Idle → 종료)이 끝까지 도는지 확인한다.

### 10-3. 사용자 수동 테스트 목록 (README에 포함)
- [ ] 시작 버튼 → 3, 2, 1 예비음 → 높은 경계음 → 첫 영역 시작
- [ ] 영역 12분 지점(종료 3분 전)에 알림음 1번, 숫자가 앰버로 변경
- [ ] 영역 종료 3초 전부터 예비음 3번 → 경계음 → 휴식 화면
- [ ] 휴식 종료 3초 전부터 예비음 3번 → 경계음 → 다음 영역
- [ ] 5번째 영역 종료 시 예비음 → 경계음 → 종료 화면
- [ ] 일시정지 후 재개하면 남은 시간이 그대로 이어지고 알림 시각도 밀린다
- [ ] 화면을 끄거나 다른 앱으로 이동해도 알림이 제때 울린다
- [ ] 소리만 / 진동만 / 둘 다 / 둘 다 끔 각각 동작
- [ ] 휴식 시간을 바꾸면(예: 2분, 0초) 다음 시험부터 반영
- [ ] 뒤로가기 시 중단 확인창, 확인하면 서비스와 웨이크락이 정리됨
- [ ] 79분 전체를 한 번 돌렸을 때 종료 시각의 오차가 1초 이내

### 10-4. 완료 기준
- 위 단위 테스트가 모두 통과한다.
- 서명된 릴리스 APK가 빌드되고 설치 후 실행된다.
- 릴리스 APK 다운로드 URL과 QR 이미지, 설치 README가 있다.
- 범위 밖 항목이 하나도 포함되어 있지 않다.

## 11. 진행 순서 (마일스톤)

1. **프로젝트 생성과 도메인 로직**: 빈 Compose 프로젝트, `Timeline`/`Alerts` 구현과 단위 테스트 통과
2. **엔진과 서비스**: 타이머 엔진, 포그라운드 서비스, 일시정지/재개, 디버그 배율로 흐름 검증
3. **소리와 진동**: 사인파 생성, 재생, 진동 패턴, 알림 규칙 연결
4. **UI**: 5개 화면, 테마(라이트/다크), 설정 저장
5. **마감**: 권한 안내, 뒤로가기 처리, 아이콘, 앱 이름
6. **배포**: keystore 생성, 릴리스 APK, 호스팅, QR, README
7. **사용자 테스트**: 수동 테스트 목록 전달 후 소리 높낮이, 진동 세기 등 피드백 반영

각 단계가 끝날 때마다 무엇을 만들었고 무엇을 확인했는지 사용자에게 짧게 보고한다.

## 12. 아직 확인되지 않은 것 (사용자에게 물어볼 것)

1. 영역 순서가 실제 시험과 같은지 (현재는 위 가정)
2. 시작 버튼 후 3초 카운트다운을 넣는 방식이 괜찮은지
3. 예비음 / 3분 전 알림음 / 경계음의 음 높이와 길이 (첫 빌드 후 들어보고 조정)
4. APK 호스팅 방식 (공개 GitHub 저장소 가능 여부)
5. 패키지명과 앱 아이콘 취향
6. 사용하는 폰의 종류(안드로이드 버전). APK 설치는 안드로이드 전용이다.
